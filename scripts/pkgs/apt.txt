# apt — OS, system integration, and anything needing root/systemd on Ubuntu.
# One package per line; '#' starts a comment. apt has no --skip-unavailable, so
# setup-ubuntu.sh retries individually when the batch fails: a name that is not
# in your release is reported and skipped rather than aborting the run.
# Repos (Microsoft, Sublime, Vivaldi, Chrome, Ulauncher PPA) are added by
# setup-ubuntu.sh before this list is installed.

# --- core ---
git
git-lfs
curl
wget
zsh
ssh-askpass
p7zip-full
p7zip-rar
unzip
jq
pandoc
graphviz
protobuf-compiler
ripgrep
fd-find                 # binary is `fdfind`
fzf
bat                     # binary is `batcat`
htop

# --- build toolchain ---
build-essential
clang
cmake
make
python3
python3-pip
python3-dev
python3-venv
software-properties-common
apt-transport-https
ca-certificates
gnupg
lsb-release
gdebi

# --- .NET (from the Microsoft prod repo) ---
dotnet-sdk-9.0

# --- editors ---
neovim
python3-neovim
neovim-qt
sublime-text            # repo: sublimehq
sublime-merge           # repo: sublimehq
code-insiders           # repo: packages.microsoft.com/repos/code

# --- git / diff ---
gh
meld
kdiff3

# --- browsers ---
microsoft-edge-dev      # repo: packages.microsoft.com/repos/edge
vivaldi-snapshot        # repo: repo.vivaldi.com
google-chrome-stable    # repo: dl.google.com/linux/chrome

# --- shells & terminals ---
powershell              # repo: packages.microsoft.com prod
terminator

# --- media ---
vlc
ffmpeg

# --- writing / LaTeX ---
texstudio
texlive-latex-extra     # swap for texlive-full if you hit missing packages
texlive-fonts-recommended

# --- desktop / GNOME ---
gnome-tweaks
gnome-shell-extension-manager
gnome-shell-extension-prefs
gnome-font-viewer
gnome-screenshot
blueman
ulauncher               # PPA: agornostal/ulauncher
polybar
conky
redshift
redshift-gtk

# --- fonts ---
fonts-powerline
fonts-firacode
fonts-noto-color-emoji
# Nerd Fonts are not packaged; setup-ubuntu.sh fetches Meslo + FiraCode.

# --- mail ---
thunderbird
evolution

# --- misc ---
keepassxc
filezilla
doublecmd-gtk
yt-dlp
lnav
pass
